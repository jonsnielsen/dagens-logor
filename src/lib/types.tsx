export enum Visibility {
  PUBLIC = "Public",
  PRIVATE = "Private",
}

export enum Category {
  CERAMICS = "Ceramics",
  OTHER = "Other",
}

export type ServerErrorType = { message: null | string };
